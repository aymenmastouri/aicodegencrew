# =============================================================================
# SDLC Pilot — Configuration
# =============================================================================
#
# Copy this file to .env and fill in your values:
#   copy .env.example .env      (Windows)
#   cp .env.example .env        (macOS/Linux)
#
# Then set at minimum: PROJECT_PATH and OPENAI_API_KEY
#
# =============================================================================

# Path to the repository to analyze
PROJECT_PATH=/path/to/your/repo

# Task and reference input directories (upload via Dashboard or place files here)
TASK_INPUT_DIR=./inputs/tasks
REQUIREMENTS_DIR=./inputs/requirements
LOGS_DIR=./inputs/logs
REFERENCE_DIR=./inputs/reference

# ── LLM Configuration ──────────────────────────────────────────────────────
LLM_PROVIDER=onprem
API_BASE=https://litellm.your-platform.example.com/v1
OPENAI_API_KEY=sk-your-api-key-here

# Qwen3-Coder-Next (80B MoE, 3B active, 262K ctx) — architecture, documentation, planning
MODEL=openai/code

# Same model for all tasks (simplifies configuration)
FAST_MODEL=openai/code

# Qwen3-Coder-Next — code generation, tool use, agentic coding
CODEGEN_MODEL=openai/code

# Mistral-Small-3.1-24B-Instruct (24B, 131K ctx, EU) — OCR, documents, multilingual
VISION_MODEL=openai/vision

# ── Token limits ─────────────────────────────────────────────────────────────
MAX_LLM_OUTPUT_TOKENS=65536
LLM_CONTEXT_WINDOW=262144

# ── Embeddings ─────────────────────────────────────────────────────────────
EMBED_MODEL=embed

# ── Output & indexing ──────────────────────────────────────────────────────
INDEX_MODE=auto
OUTPUT_DIR=./knowledge/architecture
DOCS_OUTPUT_DIR=./architecture-docs

# ── Logging & tracing ─────────────────────────────────────────────────────
LOG_LEVEL=INFO
CREWAI_TRACING_ENABLED=false

# ── Timezone (used in Docker containers) ───────────────────────────────────
# TZ=Europe/Berlin

# ── Code generation ────────────────────────────────────────────────────────
# Skip build verification (useful when build tools are unavailable)
# CODEGEN_BUILD_VERIFY=false

# ── ChromaDB (optional: client-server mode) ────────────────────────────────
# If CHROMA_SERVER_HOST is set, semantic search uses HTTP instead of local SQLite.
# CHROMA_SERVER_HOST=127.0.0.1
# CHROMA_SERVER_PORT=8005
# CHROMA_SERVER_SSL=false

# ── Dashboard port (Docker only, default: 80) ─────────────────────────────
# PORT=80

# ══════════════════════════════════════════════════════════════════════════════
# Platform Services (all optional — uncomment and configure as needed)
# ══════════════════════════════════════════════════════════════════════════════

# ── Langfuse LLM Observability ─────────────────────────────────────────────
# Traces every LLM call via LiteLLM's native Langfuse integration.
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_HOST=https://langfuse.your-platform.example.com

# ── Qdrant Vector Store (alternative to ChromaDB) ─────────────────────────
# VECTOR_DB=chroma
# QDRANT_URL=https://qdrant.your-platform.example.com
# QDRANT_API_KEY=
# QDRANT_COLLECTION=repo_docs

# ── Neo4J Knowledge Graph ─────────────────────────────────────────────────
# NEO4J_URI=neo4j+s://neo4j-bolt.your-platform.example.com:443
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=

# ── MLflow Experiment Tracking ─────────────────────────────────────────────
# MLFLOW_TRACKING_URI=https://mlflow.your-platform.example.com
# MLFLOW_EXPERIMENT_NAME=aicodegencrew

# ── Prometheus / Grafana Metrics ───────────────────────────────────────────
# PROMETHEUS_ENABLED=false

# ── MCPO HTTP Transport ───────────────────────────────────────────────────
# MCP_TRANSPORT=stdio
# MCPO_URL=https://mcpo.your-platform.example.com

# ── Docling Document Conversion ───────────────────────────────────────────
# DOCLING_URL=https://docling.your-platform.example.com

# ── Platform MCP Server ──────────────────────────────────────────────────────
# PLATFORM_MCP_URL=https://your-platform-mcp.example.com

# ── Authentik OIDC Authentication ──────────────────────────────────────────
# OIDC_ENABLED=false
# OIDC_AUTHORITY=https://authentik.your-platform.example.com/application/o/aicodegencrew
# OIDC_CLIENT_ID=
# OIDC_CLIENT_SECRET=
# OIDC_SCOPES=openid profile email
