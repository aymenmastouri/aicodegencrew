= C4 Level 3: Component Diagram


== 3.1 Overview

The backend container (`container.backend`) implements the core business capabilities of the *uvz* system. It follows a classic layered architecture (Controller → Service → Repository → Entity) built with *Spring Boot* and *JPA*. The component landscape is large (951 components) but can be summarised by stereotype:


[cols="1,1,1", options="header"]
|===
| Stereotype
| Total
| Sample Components (first 10)

| Controllers
| 32
| ActionRestServiceImpl, IndexHTMLResourceService, StaticContentController, CustomMethodSecurityExpressionHandler, JsonAuthorizationRestServiceImpl, ProxyRestTemplateConfiguration, TokenAuthenticationRestTemplateConfigurationSpringBoot, KeyManagerRestServiceImpl, ArchivingRestServiceImpl

| Services
| 184
| ActionServiceImpl, ActionWorkerService, HealthCheck, ArchiveManagerServiceImpl, MockKmService, XnpKmServiceImpl, KeyManagerServiceImpl, WaWiServiceImpl, ArchivingOperationSignerImpl, ArchivingServiceImpl

| Repositories
| 38
| ActionDao, DeedEntryConnectionDao, DeedEntryDao, DeedEntryLockDao, DeedEntryLogsDao, DeedRegistryLockDao, DocumentMetaDataDao, FinalHandoverDataSetDao, FinalHandoverDataSetDaoCustom, HandoverDataSetDao

| Entities
| 360
| ActionEntity, ActionStreamEntity, ChangeEntity, ConnectionEntity, CorrectionNoteEntity, DeedEntryEntity, DeedRegistryEntity, DocumentMetaDataEntity, HandoverDataSetEntity, NumberManagerEntity

|===


The diagram below (c4-component.drawio) visualises these layers without enumerating every individual component – instead it shows the layer boundaries and the count of components per layer.


'''


== 3.2 Backend API Components


=== 3.2.1 Layer Overview

[cols="1,1,1,1", options="header"]
|===
| Layer
| Purpose
| Component Count
| Typical Pattern

| Controllers
| HTTP request handling, input validation, response mapping
| 32
| Spring `@RestController` / `@Controller`

| Services
| Business logic, orchestration, transaction management
| 184
| Spring `@Service` (service layer)

| Repositories
| Data‑access, CRUD, custom queries
| 38
| Spring Data JPA `@Repository`

| Entities
| Domain model, persistence mapping
| 360
| JPA `@Entity`

|===


=== 3.2.2 Controllers (Presentation Layer)

*Count:* 32 controllers

[cols="1,1,1", options="header"]
|===
| Controller
| Primary Endpoint(s)
| Responsibility

| ActionRestServiceImpl
| `/api/actions/**`
| Exposes CRUD for `ActionEntity`

| IndexHTMLResourceService
| `/` (root)
| Serves the SPA entry point

| StaticContentController
| `/static/**`
| Delivers static assets

| JsonAuthorizationRestServiceImpl
| `/api/auth/**`
| Handles JSON‑based auth tokens

| KeyManagerRestServiceImpl
| `/api/keys/**`
| Key management operations

| ArchivingRestServiceImpl
| `/api/archive/**`
| Archive creation & retrieval

| ReportRestServiceImpl
| `/api/reports/**`
| Report generation & download

| JobRestServiceImpl
| `/api/jobs/**`
| Background job control

| NumberManagementRestServiceImpl
| `/api/numbers/**`
| Number allocation & gap handling

| OfficialActivityMetadataRestServiceImpl
| `/api/official/**`
| Metadata for official activities

|===


=== 3.2.3 Services (Business Layer)

*Count:* 184 services

[cols="1,1,1", options="header"]
|===
| Service
| Core Responsibility
| Key Dependencies

| ActionServiceImpl
| Business rules for actions
| ActionDao, ActionRestServiceImpl

| ArchiveManagerServiceImpl
| Coordination of archiving workflow
| ArchivingServiceImpl, Repository layer

| HealthCheck
| System health endpoints
| Spring Actuator

| MockKmService
| Mock key‑manager for tests
| No external DB

| XnpKmServiceImpl
| Real key‑manager integration
| KeyManagerDao

| WaWiServiceImpl
| Integration with external WaWi system
| RestTemplate, external API

| ReportServiceImpl
| Assemble data for reports
| Multiple repositories

| JobServiceImpl
| Scheduling & execution of background jobs
| JobDao, Scheduler

| NumberManagementServiceImpl
| Allocate, reserve, and release numbers
| UzvNumberManagerDao, UzvNumberGapManagerDao

| SignatureFolderServiceImpl
| Manage signature folder storage
| FileSystem service

|===


=== 3.2.4 Repositories (Data‑Access Layer)

*Count:* 38 repositories

[cols="1,1,1", options="header"]
|===
| Repository
| Managed Entity
| Notable Custom Queries

|===

| ActionDao | ActionEntity | `findByStatus`, `findRecent` | DeedEntryDao | DeedEntryEntity | `findByDeedNumber`, `findActive` | DeedEntryLockDao | DeedEntryLockEntity | `lockDeed`, `unlockDeed` | DocumentMetaDataDao | DocumentMetaDataEntity | `searchByTitle`, `filterByDate` | HandoverDataSetDao | HandoverDataSetEntity | `findPending`, `findByRecipient` | ParticipantDao | ParticipantEntity | `findByRole`, `findActive` | SignatureInfoDao | SignatureInfoEntity | `findBySignatureId` | UzvNumberManagerDao | NumberManagerEntity | `nextAvailableNumber` | ReportMetadataDao | ReportMetadataEntity | `findByReportId` | TaskDao | TaskEntity | `findPendingTasks`


=== 3.2.5 Entities (Domain Layer)

*Count:* 360 JPA entities (excerpt)

[cols="1,1,1", options="header"]
|===
| Entity
| Table
| Key Relationships

|===

| ActionEntity | `action` | Many‑to‑One `User`, One‑to‑Many `ActionStream` | ActionStreamEntity | `action_stream` | Many‑to‑One `Action` | ChangeEntity | `change` | One‑to‑Many `ChangeDetail`

[cols="1,1,1", options="header"]
|===
| ConnectionEntity
| `connection`
| Self‑referencing for hierarchical connections

|===

| CorrectionNoteEntity | `correction_note` | Many‑to‑One `DeedEntry` | DeedEntryEntity | `deed_entry` | One‑to‑Many `DeedEntryLog`, Many‑to‑One `DeedRegistry` | DocumentMetaDataEntity | `document_metadata` | One‑to‑Many `SignatureInfo` | HandoverDataSetEntity | `handover_dataset` | Many‑to‑One `Participant` | NumberManagerEntity | `number_manager` | Unique constraint on `number` | ParticipantEntity | `participant` | One‑to‑Many `SignatureInfo`


'''


== 3.3 Component Dependencies


=== 3.3.1 Layer Interaction Rules

[cols="1,1,1", options="header"]
|===
| From Layer
| To Layer
| Allowed?

| Controller
| Service
| ✅

| Service
| Repository
| ✅

| Service
| Service (peer)
| ✅ (via Spring DI)

| Repository
| Entity
| ✅

| Service
| Entity
| ❌ (should go through Repository)

| Controller
| Repository
| ❌ (violate layering)

|===


=== 3.3.2 Typical Request Flow

----
Client → HTTP GET /api/actions/123
   ↓
ActionRestServiceImpl (Controller)
   ↓
ActionServiceImpl (Service)
   ↓
ActionDao (Repository)
   ↓
ActionEntity (JPA Entity) ↔ Database
   ↑
Result propagates back up the same chain as JSON response
----


'''


== 3.4 Diagram Reference

The full C4 Component diagram is stored as *c4-component.drawio* in the repository. It visualises the four layers, the component counts, and the allowed dependency arrows as described above.


'''


*Document generated on 2026‑02‑09 using real architecture facts.*
