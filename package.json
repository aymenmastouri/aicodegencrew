{
  "private": true,
  "description": "AICodeGenCrew dev scripts — start/stop backend + frontend",
  "scripts": {
    "dev": "concurrently -k -p name -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "python -m uvicorn ui.backend.main:app --reload --host 127.0.0.1 --port 8001",
    "dev:frontend": "npm start --prefix ui/frontend",
    "stop": "node scripts/stop-dev.js",
    "build": "npm run build --prefix ui/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
